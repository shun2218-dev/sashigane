# sashigane

デザインシステム。**トークンが唯一の正**であり、値は規則から生成する。

このリポジトリは AI 駆動開発の実践例を兼ねている。
**設計判断の根拠と、失敗の記録が成果物の一部である。**
動くコードだけを残して経緯を捨てると、このリポジトリの価値は半分になる。

**現在 Phase 1（トークン層）。** `packages/tokens` `packages/ui` `apps/docs` はまだ存在しない。
Phase の一覧は [README](README.md#進め方)。

---

## 作業を始める前に

```bash
pnpm install     # prepare が core.hooksPath を .githooks に向ける
```

**これを実行するまで pre-push フックは有効にならない。**
`core.hooksPath` は `.git/config` に保存され追跡対象外のため、clone しただけでは存在しない。

---

## 手順（人間・AI 共通。速度を理由に飛ばさない）

1. **Issue を立ててから branch を切る。** main への直接 push は禁止
2. 実装する
3. PR を出す
4. **Strict Lead Engineer として自己レビューし、結果を `gh pr review` で PR に投稿する**
   チャット内だけのレビューはレビューとみなさない
5. 指摘を修正し、対応をコメントで記録する

コミットメッセージは**日本語の Conventional Commits**。
末尾に `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`。

**同じ問題を3イテレーション修正して直らない場合、続行せず人間に判断を仰ぐ。**

---

## 過去の失敗から来ている規則

`docs/agent-failures.md` に記録された失敗の要約。**すべて実際に起きたもの。**

### 1. 生成規則を書いたら、必ず実行して出力を決定文書と照合する

spacing の生成規則を**2度**、言葉としては筋が通るが実際には違う値を生成する形で書いた。

言葉の上で筋が通ることと、コードが期待どおり動くことは別である。
「規則から値を導く」と主張するプロジェクトで、値を目視確認せずに規則を書いてはならない。

→ `scripts/metrics/scales.mjs` は import 時に期待値を自己検査する。
**期待値は「規則から導いた値」ではなく「規則が正しいことの証人」である。**
期待値を書き換えて通すのは、規則の誤りを隠すことに等しい。

### 2. 検証は生の出力を見る。フィルタした結果だけで結論を出さない

grep が外れていて「生成されない」と**逆の結論**を出しかけた。

「該当なし」を見たら、まずフィルタが正しいかを疑う。
対象が存在しないことと、フィルタが外れていることは区別がつかない。

**「何も検出されなかった」は成功ではなく、検査が機能していない可能性を含む。**
ゼロ件の結果には、意図的に該当するケースを1件作って検査が発火することを確かめる。

### 3. 機械的に検査できるものは、文書ではなく検査にする

**この規則を文書に書いた本人が、同じコミットの中でその規則を破った実例がある。**
「書いたから守られる」は成立しない。

文書は機械化できないものにだけ使う。**文書は最後の手段であって、最初の手段ではない。**

### 4. 静かに失敗するものを疑う

設定ファイルの意味論の誤りは、動かしてもエラーにならない。誰も気づかない。

**エラーにならない仕組みほど、意図どおり動いていることを能動的に確かめる。**

### （前提）仕様は記憶で書かない。動かして確かめる

**shadcn CLI と Tailwind v4 は仕様変更が速い。** 訓練データの記憶は当てにならない。

実際に「Tailwind preset を生成する」という前提が v4 では成立しないことが判明した。
確かめた結果は `docs/experiments/` に残す。

---

## 動かせない設計原則

詳細と根拠は [docs/decisions.md](docs/decisions.md)。**ここを読まずに実装しない。**

1. **トークンが唯一の正** — 生値（`#3b82f6`、`16px`）をコンポーネントに直接書かない。生成物はコミットしない
2. **値は導出する** — 唯一の根本定数は `root = 16px`。既存プロジェクトから値を抽出しない
3. **プリミティブとセマンティックを分ける** — **コンポーネントはセマンティックしか参照できない**
   - `--sg-{category}-{数字}` = プリミティブ（参照禁止）
   - `--sg-{category}-{単語}` = セマンティック（参照可）
4. **依存は一方通行** — `packages/tokens` は React も `packages/ui` も import しない
5. **色は OKLCH で生成する** — WCAG 適合は後から直すものではなく、そうなっているもの
6. **配布は shadcn レジストリ方式** — トークンのみのインストールが動くことが層分離の証明
7. **コンポーネントは実需要のあるものだけ** — 「あった方が良さそう」で追加しない

### ガードレールを足すときの順序

**必ず `docs/agent-failures.md` に理由を書いてから追加する。**

ルールが先にあってはならない。失敗が先にあり、ルールはその対処として生まれる。
理由の書かれていないルールは、後から誰も消せなくなる。

---

## UI コンポーネントを触るとき

Storybook のストーリー3状態（**通常 / 空 / エッジケース**）を必ず添える。
**ストーリーがない時点で自己レビュー不合格。**

---

## ドキュメントの地図

| ファイル | 内容 |
|---|---|
| [docs/decisions.md](docs/decisions.md) | 設計決定。生成規則・値・退けた案・改訂履歴 |
| [docs/roles.md](docs/roles.md) | 既存4本から観測したセマンティック役割 |
| [docs/verification.md](docs/verification.md) | 生成スケールが実需要を覆えるかの検証 |
| [docs/agent-failures.md](docs/agent-failures.md) | 失敗とガードレールの由来 |
| [docs/experiments/](docs/experiments) | 仕様を推測せず動かして確かめた記録 |

## よく使うコマンド

```bash
pnpm test                     # スケールの不変条件を検査する（decisions.md との一致を含む）
pnpm typecheck
pnpm check:tokens-isolation   # packages/tokens が単体で成立していることを検査する（原則4）
pnpm check:lessons            # agent-failures.md の教訓がこのファイルに届いているか検査する

pnpm scales                   # スケールを表示する（人が目で見るためのもの。検査ではない）
pnpm verify:coverage          # 実需要に対するカバー率（観測対象4本のローカル clone が必要）
```

CI は `verify:coverage` を除く全部を走らせる。
`verify:coverage` は観測対象4本のローカル clone を前提とするため CI では動かない。
**スケールを変更したら手元で実行し、`docs/verification.md` の数値を更新すること。**
