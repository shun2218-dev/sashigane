/*
 * ── 維持する側への覚書 ───────────────────────────────
 *
 * 押せるものの焦点の線。**コンポーネントではないので例も展示ページも持たない。**
 * 検査はコンポーネントを `<name>/<name>.tsx` で数えているため、ここは対象外である。
 *
 * ## 入力の線（`ring.ts`）とは別物である
 *
 * 入力の線は**状態（誤り・満たす）も表す**ので、枠の側に置いて `has-focus-visible`
 * で拾う。こちらは**押せるもの自身に付く**線で、状態を持たない。
 *
 * ## 1箇所に置いている理由
 *
 * ボタン・知らせの枠の閉じる釦・暦の日と月送りが、同じ線を描く。
 * **写しを作ると、片方だけ直したときにずれる。**
 * ここにあるのは線だけで、箱の形（大きさ・角・余白）は呼ぶ側が足す。
 * ─────────────────────────────────────────────
 */

/**
 * 焦点の線。**輪郭で描く。**
 *
 * 境界の幅を変えると箱の寸法が変わり、焦点が移るたびに下にあるものがずれる。
 * 輪郭は描画だけで寸法に関わらない。
 */
export const FOCUS_RING =
  'focus-visible:outline-solid focus-visible:outline-2 ' +
  'focus-visible:outline-offset-2 focus-visible:outline-border-focus';
