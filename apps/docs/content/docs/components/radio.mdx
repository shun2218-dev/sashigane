---
title: Radio
description: いくつかから1つを選ぶ。群れの札は fieldset と legend で作る
---

## 群れの札が要ります

1つずつの札（「ふつう」「大きい」）だけでは、
**何についての選択なのかが読み上げに出ません。**

群れの札は `fieldset` と `legend` でしか作れません。`div` に `aria-label` を足す形も
動きますが、**`legend` は選択肢を読むたびに群れの札を添えます。**

```tsx
<RadioGroup id="plan" label="プラン">
  <Field layout="inline" id="plan-a" label="ふつう">
    <Radio name="plan" value="a" />
  </Field>
  <Field layout="inline" id="plan-b" label="大きい">
    <Radio name="plan" value="b" />
  </Field>
</RadioGroup>
```

## name は利用側が渡します

群れから配っていません。配るには文脈（context）が要り、
**この部品がクライアント側になります。** いまはどの部品もサーバ側で描けます。

`name` が揃っていないと、**全部が独立に選べる状態になります。**
これはテストが測っています。

## 誤りは群れに付きます

選ばれていないのは選択肢1つの問題ではないので、1つずつに赤い線を引いても
**どれを直せばよいか伝わりません。**

<ComponentDemo name="radio" />
