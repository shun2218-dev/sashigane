---
title: Accordion
description: 折りたたみ。素の details に乗っていて、状態を1つも持たない
---

## 状態を持ちません

**素の `details` / `summary` に乗っています。** 開閉も鍵盤も読み上げも、
ブラウザが既に持っています。

```tsx
<Accordion>
  <AccordionItem>
    <AccordionTrigger>見出し</AccordionTrigger>
    <AccordionContent>中身</AccordionContent>
  </AccordionItem>
</Accordion>
```

`aria-expanded` は書いていません。**ブラウザが既に申告しており、書くと二重になります。**

## 一度に1つだけ開く

**同じ `name` を升に渡してください。** これもブラウザが面倒を見ます。

```tsx
<AccordionItem name="faq">…</AccordionItem>
<AccordionItem name="faq">…</AccordionItem>
```

器が名前を作って配る形にはしていません。安定した名前を作るには `useId` が要り、
**器がクライアント側の部品になる**ためです。いまの部品はどれもサーバ側で描けます。

## 中身は閉じていても存在します

`details` は閉じていても中身を持ちます。**検索で見つかり、印刷にも出ます。**

描かないようにしたいときは、**利用側が中身の側で決めてください。**

## 既定の三角は出ません

`summary` の三角は `display: list-item` に付くので、**`flex` にすると出なくなります。**

`list-style` だけでは Safari で残ります。効いているのは `flex` の方です。

<ComponentDemo name="accordion" />
